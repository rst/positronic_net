<org.positronicnet.sample.contacts.PhotoEditor
   xmlns:android="http://schemas.android.com/apk/res/android"
   android:orientation="vertical"
   android:layout_width="fill_parent"
   android:layout_height="wrap_content">
  <include layout="@layout/separator_bar"/>
  <LinearLayout android:layout_width="fill_parent"
                android:layout_height="wrap_content">
    <!-- Stack buttons on the left -->
    <LinearLayout android:orientation="vertical"
                  android:layout_width="wrap_content"
                  android:layout_height="wrap_content">
      <TextView android:id="@+id/section_header"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_weight="0"
                android:text="@string/photo_header"
                android:paddingRight="10dp"
      />
      <org.positronicnet.ui.PositronicButton
                android:id="@+id/snapPhoto"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/snap_photo"
      />
      <org.positronicnet.ui.PositronicButton
                android:id="@+id/deletePhoto"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/delete_photo"
      />
    </LinearLayout>
    <!-- The image itself on the right -->
    <ImageView android:id="@+id/image"
               android:minWidth="96px"
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"/>
  </LinearLayout>
</org.positronicnet.sample.contacts.PhotoEditor>
